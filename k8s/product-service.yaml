apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: prog3360-assignment3
spec:
  selector:
    app: product-service
    version: blue
  ports:
    - port: 8081
      targetPort: 8081
